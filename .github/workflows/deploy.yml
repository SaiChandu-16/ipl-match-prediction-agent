name: Deploy
on:
  push:
    branches: [main]
    paths: ['agent/**','Dockerfile']
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render
        run: curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"
